# 行星科学知识网站 · 架构与设计元素分析报告（更新版）

> 分析对象：当前工作区 `Kimi_Agent_行星科学网站框架`
> 分析日期：2026-07-27
> 状态更新：**已决策 —— 以 `app/`（React 体系）为唯一主体**，原 VuePress 冗余部分（共 6 项，约 1.1MB）已于 2026-07-27 移入回收站（`~/.Trash/Kimi_Agent_redundant_20260727/`，可恢复），工作区现已收敛为单一实现。

---

## 0. 一页结论（TL;DR）

| 维度 | 当前唯一实现：`app/` |
|---|---|
| 形态 | React 19 + Vite 7 **单页应用 SPA**（沉浸式、可视化能力强） |
| 设计语言 | **Sanctuary**：暗色深空、古铜金、电影感、3D |
| 内容来源 | `src/content/**/*.md`（**现已成为唯一内容来源**） |
| 渲染 | 客户端渲染（CSR）+ 运行时 `fetch` md（JS 依赖强，SEO 待补） |
| 落地完整度 | 高（有 `dist/` 构建产物、60+ UI 组件、3D/图表/命令面板） |

**当前核心议题已转变**：从"二选一/融合"变为"如何在 React 单体系下补齐短板、统一与深化 Sanctuary 设计语言"。双品牌冲突、内容双份分叉等问题已随清理而自然消除。

---

## 1. 仓库拓扑（当前状态）

```
Kimi_Agent_行星科学网站框架/
├── app/                          ← 唯一前端工程：React 19 + Vite 7 SPA
│   ├── src/
│   │   ├── pages/                ← 9 个路由页面
│   │   ├── components/           ← Layout / Hero 系列 / 3D / MarkdownRenderer
│   │   ├── components/ui/        ← 60+ shadcn/ui 组件
│   │   ├── components/backgrounds/ ← 6 个 3D/粒子场景组件
│   │   ├── lib/content.ts        ← Markdown 内容加载器（import.meta.glob）
│   │   └── content/              ← 知识/学者/资源/新闻 的 .md 内容（单一来源）
│   ├── dist/                     ← 已构建的静态产物（可直接部署）
│   └── package.json / vite.config.ts / tailwind.config.js / index.html
│
├── 架构与设计分析报告.md          ← 本报告
├── skills-lock.json              ← WorkBuddy 技能清单（非网站内容，保留）
└── .workbuddy/                   ← 工作记忆目录
```

**已移除（移入回收站，可恢复）**：`planetary-science-fixed/`、`Astrobiology-cn.github.io-main/`、`vuepress-theme-package/` 及 `vuepress-theme-package.zip`、`repo.zip`、`fix.patch` —— 均为原 VuePress 体系及其打包/补丁，与 React 实现无关。

**内容现状**：`app/src/content/` 现为内容唯一来源。此前与 VuePress `docs/` 结构镜像但正文分叉的双份问题，已随对方删除而消除，后续只需维护这一份。

---

## 2. 架构分析（React 19 + Vite 7 SPA）

**技术栈**：`react@19` + `react-dom@19` + `vite@7` + `tailwindcss@3` + **shadcn/ui**（Radix 全套）+ `react-router@7`（**HashRouter**）+ `react-markdown` + `remark-gfm/math` + `rehype-katex/raw` + `gray-matter`（已装但**未用**）+ `fuse.js`（搜索）+ `three` / `@react-three/fiber` / `@react-three/drei` / `vanta`（3D）+ `gsap` + `lenis`（平滑滚动）+ `recharts`（图表）+ `cmdk`（命令面板）+ `next-themes`（主题切换）。

**内容模型（双机制，需警惕）**
- `lib/content.ts` 用 `import.meta.glob('../content/**/*.md', { eager, as:'raw' })` 在**构建期**把 md 打进 JS，用于导航、卡片、学者/资源/新闻列表等**元数据**。
- 但真正的**正文渲染**走 `MarkdownRenderer.tsx`：`useEffect` 里 `fetch(url)` 在**运行时**按 URL 拉取 `.md` 文本——意味着知识正文依赖浏览器联网拉取，且要求 md 以静态资源形式可被访问。
- 自带一个脆弱的**手写 YAML frontmatter 解析器**（`parseFrontmatter`），而依赖里明明装了更稳的 `gray-matter` 却没用它。

**路由**：`react-router` + `HashRouter`，显式路由（`/knowledge`、`/knowledge/:subjectId`、`/scholars/:scholarId`、`/resources/:resourceId`、`/news/:id`、`/about`）。`HashRouter` 对静态托管友好（无需服务端 rewrite）。

**渲染**：纯客户端。Markdown → `react-markdown`（GFM + KaTeX + 原始 HTML）。Obsidian Callout 用正则预处理成 `data-callout` 占位再渲染。KaTeX 公式、表格、代码块均自定义样式。

**主题（Sanctuary 设计语言）**：`index.css` 用 HSL CSS 变量定义浅色（parchment）与深色（abyss 深空蓝 `#080E21`）；`tailwind.config.js` 把 shadcn 的 `background/foreground/primary/...` 映射到这些变量。`index.html` 的 `theme-color` 为 `#030818`；首页系列 Hero（`PlanetHero`/`VantaHero`/`InkHero`/`OrbitEntry`/`RocketManifesto`/`PlanetCanvas` + `backgrounds/` 下 6 个场景组件）均为暗色、3D、电影感。

**部署**：`vite build`（已生成 `dist/`），`base: './'` 相对路径，可丢到任意静态服务器；不依赖特定平台。

**优点**：交互/可视化天花板极高（3D 行星、粒子、平滑滚动、图表、命令面板）；设计语言统一可程序化（CSS 变量）；自托管字体（fontsource，无外部 CDN 依赖）。

**风险（详见第 4 节）**：
1. 首屏/SEO 弱：内容靠运行时 fetch + JS 渲染，无预渲染时搜索引擎/分享卡片抓不到正文。
2. 内容管线双轨：`import.meta.glob`（构建期）与 `fetch`（运行时）并存，易不一致。
3. 手写 frontmatter 解析器脆弱，建议直接换 `gray-matter`。
4. 3D/动画性能与可访问性：已做 `prefers-reduced-motion` 兜底（很好），但 Three.js/Vanta 在低端设备仍可能卡顿。

---

## 3. 设计元素分析（Sanctuary 设计语言）

> 原 VuePress 侧的 **FreshKnowledge**（暖白/青绿/Inter）设计系统已随冗余项移除，不再作为本项目的并行语言。下方仅保留当前生效的 Sanctuary 规范，作为后续深化的基准。

### 3.1 Sanctuary（落地于 React SPA）
- **氛围**：暗色、沉浸式、像一座"太空圣殿"。巨型标题、极简线框几何、极端留白、缓慢稳定运动。
- **配色**：浅色 parchment `#F5F3EE` / **深色 abyss `#080E21`**（签名模式）；单一强调 **copper 古铜金 `#EA6D15`**（`#A68A58` 描边行星 logo）；线框几何用前景色 9% 透明度。
- **字体**：`Noto Serif SC`（展示/标题衬线）+ `Geist Variable`（正文）+ `Geist Mono`（代码）。**全部自托管**（fontsource）。
- **形状**：近乎直角（radius 仅 6px），细线边框、无阴影卡片（`.card-space` 仅 1px 边 + hover 描边变铜）。
- **质感**：玻璃拟态（`glass-panel`/`glass-panel-raised`，`backdrop-blur`）、空心描边巨字（`.text-outline`）、标签牌（11px 大写宽字距 `.label-plate`）、流体巨型标题（`.mega-title` `clamp(3rem, …, 10rem)` 全大写）。
- **动效**：缓慢、有仪式感（`--ease-sanctuary` 等 expo 缓动），fade-up / scale-in；滚动平滑（Lenis）+ GSAP + 3D 背景（Vanta/Three）。
- **首页**：多套 Hero 组件叠加（行星画布、星座、流星、公式粒子、数据网格等），电影级入场。

### 3.2 设计令牌速查（Sanctuary）

| 元素 | 取值 |
|---|---|
| 主基调 | 暗 / 深空蓝 abyss `#080E21`（浅色 parchment `#F5F3EE` 为备选） |
| 强调色 | 单一古铜金 copper `#EA6D15`（描边 `#A68A58`） |
| 标题字体 | Noto Serif SC（衬线，自托管） |
| 正文字体 | Geist Variable（自托管） |
| 代码字体 | Geist Mono |
| 圆角 | 6px 近乎直角 |
| 阴影 | 基本无（靠 1px 描边） |
| 标志质感 | 玻璃拟态 + 线框 + 描边巨字 |
| 动效 | 缓慢仪式感 + 3D/平滑滚动（尊重 `prefers-reduced-motion`） |
| 首页 | 全屏 3D + 巨型标题 |

---

## 4. 关键发现与风险（当前体系下）

1. **SEO/首屏/深链短板（最高优先级）**：React 版正文靠运行时 `fetch` + CSR，无预渲染时不利于搜索收录与社交分享卡片。需补 SSG/预渲染（见第 5 节）。
2. **内容管线双轨**：`import.meta.glob`（构建期，元数据）与 `fetch`（运行时，正文）并存，易在不经意间不一致；建议统一。
3. **脆弱的内容解析**：React 版自写 `parseFrontmatter`，而 `gray-matter` 已装未用——建议替换。
4. **版本风险**：React 19 + Vite 7 为较新组合，需关注生态兼容性（shadcn/ui、react-router 7 等）。
5. **可访问性做得不错**：React 版已实现 `prefers-reduced-motion` 全局降级；3D 需关注低端设备降级策略。
6. **品牌命名残留**：历史配置中仍见 `Astrobiology-cn` 类引用（GitHub repo、hostname 等）的痕迹需注意——虽 VuePress 侧已删，但 React 工程内若有遗留引用应归一为现项目名。

> 已消除的历史问题：双品牌视觉冲突（亮暖 vs 暗冷）、内容双份分叉、VuePress 2 rc 版本风险——均随清理完成而不复存在。

---

## 5. 建议的下一步（在 React 单体系下深化）

**A. 补 SEO / 首屏（最关键）**
- 引入预渲染：用 `vite-plugin-ssr` / `@preactjs/renderer-preact` 思路或 `vite build` + 静态预渲染（如 `vite-plugin-prerender`、`react-snap`）生成静态 HTML 壳；
- 或将正文改为构建期 `import.meta.glob` 直接打包（与元数据同机制），消除运行时 `fetch` 依赖，顺带解决管线双轨。

**B. 统一内容管线**
- 推荐：正文与元数据**全部走 `import.meta.glob`**（构建期打包），删掉 `MarkdownRenderer` 里的运行时 `fetch`；
- 用 `gray-matter` 替换手写 `parseFrontmatter`，稳健解析 frontmatter。

**C. 深化 Sanctuary 设计语言**
- 以本报告第 3 节令牌表为准，审计 60+ UI 组件是否全部对齐（尤其卡片、按钮、表单、代码块）；
- 产出一份可复用的设计令牌文档（导出为 CSS 变量 + Tailwind 配置说明），固化品牌。

**D. 品牌命名归一**
- 全仓搜索 `Astrobiology-cn` / 旧 repo 名残留，统一为现项目名，避免历史痕迹。

**E. 内容运营**
- `app/src/content` 作为单一来源，建立写作/审校规范；如需 Markdown 写作体验，可保留 Obsidian 但只指向这一份。

---

## 6. 本分析依据的关键文件
- `app/package.json`、`vite.config.ts`、`tailwind.config.js`、`index.html`、`src/index.css`、`src/App.tsx`、`src/main.tsx`、`src/components/Layout.tsx`、`src/lib/content.ts`、`src/components/MarkdownRenderer.tsx`
- `app/src/content/`（知识/学者/资源/新闻 `.md`，现唯一内容来源）
- 目录与内容结构比对（`find` 输出）、回收站移除清单（`~/.Trash/Kimi_Agent_redundant_20260727/`）
