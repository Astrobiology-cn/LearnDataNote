# 首页线稿统一绘制规范（kimi 绘制任何 2D 图形的必须遵守）

> 配套文件：`首页场景修订简报_kimi_code.md`（本规范是简报里「线稿纪律」的**可执行版**与**几何配方库**）。
> 读者：kimi code。凡是首页 canvas 里出现的图形——火箭、工具箱、射电望远镜、卫星、飞船、信号接收器、学者北斗、望远镜——**都必须照本规范绘制**，不允许各场景各画一套。
> 核心诉求（用户原话）：望远镜线稿满意，但火箭 / 工具箱 / 射电望远镜 / 卫星 / 飞船 / 接收器线稿都不满意，**如何让 kimi 保持统一风格绘制出高质量线稿**——本规范就是答案。

---

## 0. 为什么要这份规范（现状问题）

现状：望远镜（`scopeLineArt.ts`）自有一套 `stroke/seg/ring/V3投影` 原语；射电望远镜（`ResourcesScene.drawTelescope`）又重写一套；卫星/飞船甚至没有线稿（卫星是 `arc` 圆点、飞船是 `drawImage(rocket.svg)` 实心剪影）。**没有共享绘制层 → 线宽、端点、对称、透视各不一致 → 视觉语言分裂**。

解决：**新建唯一共享绘制模块，全站所有线稿都从它出笔**。这是统一风格的工程前提，不是可选项。

---

## 1. 唯一共享绘制模块：`app/src/components/backgrounds/scenes/lineArt.ts`

必须新建此文件，把"一支笔"收口到这里，所有场景 `import` 后使用，删掉各场景里重复的 `stroke/seg/ring` 私有实现。

```ts
// lineArt.ts —— 全站 2D 线稿唯一绘制层
type V3 = { x: number; y: number; z: number };

/** 三档线宽 / 透明度（与望远镜 scopeLineArt 实测一致，全站唯一真值） */
export const LW = { OUTLINE: 1.5, STRUCT: 1.0, TEXTURE: 0.5 } as const;
export const ALPHA = { OUTLINE: 0.9, STRUCT: 0.5, TEXTURE: 0.25 } as const;

/** 一次描边：设置线宽+透明度后 stroke，自动复位 globalAlpha */
export function stroke(ctx: CanvasRenderingContext2D, color: string, lw: number, a: number): void;

/** 两点线段（已 moveTo+lineTo，调用方负责 beginPath） */
export function seg(ctx: CanvasRenderingContext2D, ax: number, ay: number, bx: number, by: number): void;

/** 圆环采样进当前路径（转向观察者投影为正圆；n=分段数） */
export function ringPath(ctx: CanvasRenderingContext2D, cx: number, cy: number, r: number, n?: number): void;

/** 椭圆环（射电碟面、电波等用），支持倾角 tilt */
export function ellipsePath(ctx: CanvasRenderingContext2D, cx: number, cy: number, rx: number, ry: number, tilt?: number, n?: number): void;

/** 弱透视投影：z 朝观察者为正，近大远小。F 为焦距（取基准尺寸的倍数） */
export function makeProjector(px: number, py: number, F: number): (p: V3) => { x: number; y: number };

/** 向量小工具（add/sub/mul/cross/norm），需要体积感的对象（火箭/卫星/飞船）用 */
export const V: {
  add(a: V3, b: V3): V3; sub(a: V3, b: V3): V3; mul(a: V3, k: number): V3;
  cross(a: V3, b: V3): V3; norm(a: V3): V3;
};

/** 通用初始化：round 连接 + 设定描边色（每帧 draw 开头调用一次） */
export function beginLine(ctx: CanvasRenderingContext2D, color: string): void;
```

要求：
- 所有场景绘制函数签名统一为 `drawXxxLineArt(ctx, opts)`，opts 含 `{ cx, cy, scale, main, copper, alpha, ... }`，**尺寸全部以 `scale` 为基准单位相对坐标**，严禁场景里写死绝对像素（如 `58 * TS` 这类要收到 opts.scale 里）。
- 望远镜 `scopeLineArt.ts` 改为从 `lineArt.ts` 引入 `stroke/seg/ringPath/V`；射电望远镜 `drawTelescope`、新增的火箭/卫星/飞船/接收器全部用同一套原语。

---

## 2. 线稿纪律（硬性，所有对象一致）

1. **仅描边 + 透明填充**：图形本身是线条；只有"刻意遮挡后方穿帮线"或"强调块"才用实色填充（如射电碟面内填背景色、工具箱箱内铜橙微光）。
2. **三档线宽 / 透明度**（全站唯一真值，见 §1 `LW`/`ALPHA`）：
   - 轮廓 OUTLINE：`lineWidth 1.5`，`alpha 0.9`
   - 结构 STRUCT：`lineWidth 1.0`，`alpha 0.5`
   - 纹理 TEXTURE：`lineWidth 0.5`，`alpha 0.25`
3. **`lineJoin = 'round'`、`lineCap = 'round'`**（每帧 `beginLine` 设好）。
4. **端点落在轮廓上，无悬空出头**：线段起点必须落在父轮廓边缘（如望远镜三脚架从枢轴球边缘起笔，不从球心飘出）。
5. **严格左右对称**：凡有中轴线的对象（火箭、飞船、卫星、射电碟、工具箱），左右结构必须镜像对称。
6. **相对坐标 / 基准单位**：所有尺寸是基准单位的倍数（如火箭总高 9U、卫星 bus 2U），靠 `opts.scale` 统一缩放，保证不同场景同一对象比例一致。
7. **透视**：需要体积感的对象（火箭箭体、卫星本体、飞船舱）用 §1 `makeProjector` 弱透视（`s = F/(F+z)`，F 取基准 6×），筒口/舱口转向观察者时投影为正圆。

---

## 3. 铜橙纪律（与修订简报 §0.2 对齐）

- 铜橙 `#EA6D15` 是**唯一**强调色，两模式恒定。
- **唯一受认可常驻点缀**：望远镜（KnowledgeScene 折射望远镜）寻星镜端点那一颗铜橙点——用户满意，**保留不动**。
- **其余所有对象默认用主题前景色 `--foreground`**；铜橙只出现在：hover / 激活 / 被电波扫到的脉冲瞬间。
- **取色走 CSS 变量**（参照已删除的 `themeCanvas.ts` 思路，用 `getComputedStyle` 读 `--foreground`/`--primary`/`--background`，监听 `<html>.dark` 的 `MutationObserver` 实时重取），**禁止写死十六进制**。射电馈源舱的铜橙菱形是身份点缀可常驻（与望远镜 finder 同类），但其它新对象不得自行加常驻铜橙。
- 学者北斗（ScholarsScene）：默认前景色，仅 hover 七星形才转铜橙（见修订简报 §4.3）。

---

## 4. 各对象几何配方（归一化坐标，kimi 照描）

> 约定：每个对象定义一个基准单位 `U`（相对坐标），实际像素 = `U × opts.scale`。kimi 在纪律内设计细节，但**必须包含下列"必备特征"**，缺一项即不合格。

### 4.1 火箭（第二幕，替换 `drawImage(rocket.svg)` 实心剪影）
母题："远行的舟 / 方舟（递给未来的信）"，安静、修长、优雅，非 aggressive 导弹感。
- **箭体**：纺锤/胶囊形，头锥尖、尾段收口；轮廓 1.5。比例：总长 ≈ 9U，最大直径 ≈ 1.5U，头锥 ≈ 1.6U。
- **尾翼**：左右各一片对称梯形/三角翼，结构 1.0；翼高 ≈ 1.2U，根部贴箭体尾段。
- **舷窗**：沿中轴 2–3 个圆（结构 1.0），均匀分布。
- **引擎喷口**：尾部钟形/喇叭口（轮廓 1.5），内画 1–2 道纹理环（0.5）。
- **腹部宣言区**：软边矩形镂空框（结构 1.0），铜橙**细**描边（身份铭文，可常驻但克制，参照望远镜 finder 铜橙）；宣言文字排印见修订简报 §3.2（用站点衬线字体，铜橙描边）。
- 左右严格对称；描边色 `main`；除腹部铭文外无铜橙。
- 动效与景深见修订简报 §3.3（从 Hero 星球背后飞出 + 透视顺滑飞近）。

### 4.2 工具箱（第三幕，绕后铰链翻开）
- **箱体**：圆角矩形（轮廓 1.5），表面分块纹理线（0.5）。
- **提手/锁扣**：顶部拱形提手（结构 1.0）+ 正面锁扣两点。
- **箱盖开合（用户重点）**：铰链在**后下沿**（非 50% 100% 底面中心），绕该铰链**向后翻转**打开，开合角 ≈ -100°~-110°（用 2D `rotate` 绕铰链点，或 `rotateX` 视实现）；打开后露出箱内。
- **箱内**：铜橙径向微光 `fillStyle = rgba(copper, 0.25)` + 2–3 件工具剪影线（扳手/直尺/齿轮，结构 0.5）。
- 动效：hover 时箱盖翻开 + 箱内铜橙微光 + 浮现"进入学科知识"（门控逻辑见修订简报 §5.2）。

### 4.3 射电望远镜碟（第五幕，重绘 `drawTelescope` —— 用户不满意项）
- **保留现有好结构**：地面线、基座、收分塔架（立柱+水平横杆+X 对角桁架，严格对称）、轴承座（背景色填实遮接点）、配重臂、碟面（内填背景色遮后方塔架穿帮）、抛物弧肋（从毂辐射、端点落外缘）、馈源支撑杆、馈源舱铜橙菱形（身份点缀，保留）。
- **重画要点**：统一改用 §1 `lineArt.ts` 原语（线宽三档与望远镜一致）；碟面比例微调得更扁更优雅（rx:ry ≈ 3:1）；肋条严格从毂辐射；所有尺寸收进 `opts.scale` 相对坐标。
- 不与第三幕折射望远镜雷同，但**同一支笔**（线宽/端点/对称/透视一致）。
- 电波改同心圆见修订简报 §6.1；粒子发射前保持旋转曲线见 §6.2。

### 4.4 卫星（第五幕，替换 2.2px 圆点）
放大并有像样线稿造型（用户："卫星造型需要适当调整，尤其是卫星"）。
- **本体 bus**：矩形或六边形舱（轮廓 1.5），表面设备分块纹理线（0.5）。
- **太阳能板**：左右各一，板面网格线（0.5），板与本体用结构臂（1.0）连接。
- **天线**：高增益小碟（圆+馈源杆，结构 1.0）或鞭状天线（1.0）。
- **比例**：bus ≈ 2U×2.5U，每侧板 ≈ 4U×1.5U，板距 0.5U；整体宽 ≈ 11U，远比现圆点大。
- 颜色 `main`；被电波扫到时铜橙回闪（保留现有 `flash` 行为，仅回闪用铜橙）。

### 4.5 飞船 / 深空探针（第五幕，替换 `drawImage(rocket.svg)` 16×16 sprite）
- **本体舱**：十边形/多面舱（轮廓 1.5）。
- **高增益天线**：碟形（结构 1.0）+ 馈源杆。
- **鞭状天线**：1–2 根（0.5/1.0）。
- **RTG 支臂**：末端方格（1.0）+ 散热鳍（0.5）。
- **引擎**：尾部 2–3 个小喷口（轮廓 1.5）。
- **比例**：舱径 ≈ 4U，含天线总长 ≈ 10U；渲染放大到 ≈ 28–36px（修订简报 §6.3）。
- 改为 canvas 线稿函数 `drawShipLineArt(ctx, opts)`，不再 `drawImage` 任何 SVG；hover/被电波扫到才转铜橙（巡航态用 `main`）。

### 4.6 信号接收器入口图标（第五幕，换新造型）
替换 `ResourcesScene` 末尾内联 `<svg class="receiver">`（当前为简易碟+桅杆）。
- **新造型**：抛物面碟 + 馈源杆 + 基座，或更精致的信号塔/天线母题，与射电望远镜呼应但不雷同；仍表达"接收深空信号"。
- **线稿纪律**：轮廓 1.5 / 结构 1.0 / 纹理 0.5，round 连接；默认 `currentColor`（muted），hover 转 `text-primary`（铜橙）。
- **保留交互动画**：指示灯慢闪 2s、hover 碟面微扬 + 铜橙急闪 0.4s + "进入外部资源"文字浮现（现有 `<style>` 逻辑不动，仅换 path 造型）。
- 建议 viewBox 内用相对坐标重画，确保与 §4.3 射电碟同一笔触。

### 4.7 学者北斗（第四幕，仅 hover 才亮铜橙）
- 七星形线稿（轮廓 1.5），背景星不连网；默认 `--foreground`，**仅 hover 七星形**转铜橙并浮现"进入学者信息"（修订简报 §4.3）。

---

## 5. 深 / 浅双模式取色（每项必验）

- 颜色由 CSS 变量驱动（§3）：`main = getComputedStyle(html).getPropertyValue('--foreground')`；浅色模式自动得到深墨前景、深色模式自动得到白前景，**不要写死 `#0B1533`/`#fff`**。
- `bg`（用于碟面内填实遮挡、基座填充）取 `--background`。
- 铜橙取 `--primary`（`#EA6D15`，两模式恒定），或写死三元组 `'234,109,21'`（恒定，可接受，但不如变量法统一）。
- 每个对象改完，必须在深色（`#080E21`）与浅色（`#F5F3EE`）各渲染验证一次：线稿清晰、遮挡正确、铜橙仅 hover 出现。

---

## 6. 与修订简报的对应

| 本规范 | 修订简报条目 | 对象 |
|---|---|---|
| §1, §2, §3 | §0 全局纪律 | 全部线稿 |
| §4.1 | §3.1, §3.2, §3.3 | 火箭 |
| §4.2 | §5.2 | 工具箱 |
| §4.3 | §6（电波/粒子另见 §6.1/§6.2） | 射电望远镜 |
| §4.4, §4.5 | §6.3 | 卫星 / 飞船 |
| §4.6 | §6.5 | 信号接收器 |
| §4.7 | §4.3 | 学者北斗 |
| §5 | §0.3 | 深浅双模式 |

> 取色去硬编码、统一共享模块，是"统一风格"的两大工程支点；几何配方（§4）是"高质量"的落地标准。两者齐备，kimi 绘制的任意对象才会像同一支笔画出。
