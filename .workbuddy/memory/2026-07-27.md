# 2026-07-27

## 行星科学网站 · 架构与设计分析
- 工作区并存两套实现：
  - `planetary-science-fixed/`（+`Astrobiology-cn.github.io-main/`）：VuePress 2 静态文档站，设计语言 FreshKnowledge（暖白/青绿/Inter+Crimson Pro），含完整 DESIGN.md/WORKFLOW.md，部署 gh-pages。
  - `app/`：React19+Vite7+Tailwind3+shadcn/ui SPA，设计语言 Sanctuary（暗色深空/古铜金/Geist+Noto Serif SC，3D/GSAP/Lenis），含 dist 构建产物。
- 关键冲突：同一品牌下两套视觉语言完全相反；内容被复制为两份（docs 与 src/content）且已分叉；React 版正文靠运行时 fetch + CSR（SEO 弱），VuePress 为 SSG（SEO 好）；React 版自写 frontmatter 解析器而未用已装的 gray-matter。
- 交付：完整分析写入 `架构与设计分析报告.md`，给出二选一/融合 + 内容单一来源 的建议。

## 决策：选用 React 体系，清理冗余
- 用户拍板：**以 `app/`（React 体系）为唯一主体**，删除其余 VuePress 冗余项。
- 已将 6 项移入回收站（可恢复，非永久删除）：`~/.Trash/Kimi_Agent_redundant_20260727/` 下含 planetary-science-fixed/、Astrobiology-cn.github.io-main/、vuepress-theme-package/ 及 vuepress-theme-package.zip、repo.zip、fix.patch。
- 保留项：app/(596M)、skills-lock.json（WorkBuddy 技能清单，非网站内容）、架构与设计分析报告.md、.workbuddy/。
- 现状：`app/src/content` 现为内容单一来源；后续建议——全面贯彻 Sanctuary 设计语言、补 SSG/预渲染以增强 SEO、用已装的 gray-matter 替换手写 frontmatter 解析器。

## 文档更新
- 重写 `架构与设计分析报告.md`：由"两套并存对比"改为"React 单体系现状 + Sanctuary 设计语言"。保留架构/设计细节，删除 FreshKnowledge 并行描述，第 4/5 节风险与下一步改为在 React 单体系下深化。文件位于工作区根目录。

## 调研：浙大猫学长(zjucat)的 VibeMotion 工程
- 仓库 `github.com/vibe-motion/vibe-motion.github.io`：纯静态官网（index.html+styles.css+app.js+server.js），无框架无打包，仅 CDN 引 GSAP 3.12.7，GitHub Pages 直部署。是 `npx create-vibe-motion` / `npx skills add vibe-motion/skills` 的落地页。
- 核心技法：像素分层标题(shadow+main+cloneNode 成 N 条 clip-path 垂直切片)+ 单 GSAP timeline(`impact` 标签 + stagger 确定性编排)+ 白点冲击/12 粒子爆 + 终端卡片一键复制(Clipboard API+execCommand 降级) + `.has-js` 门控/`sr-only` h1/`prefers-reduced-motion` 渐进增强。
- 产出 `vibemotion_适配设想.md`：把上述技法映射到本站(app/)——<ImpactTitle> 组件、统一 Hero 时间线、<TerminalCard>(本地运行/BibTeX 复制)、星爆反馈(古铜金粒子)、轻量落地页、补 SEO。强调换皮(翡翠绿→古铜金)保留机制。

## 首页场景开发提示词
- 用户要求以"多个自然段"形式写中文提示词，充分描述首页下滑的六幕场景(便于后续续写代码)，参考 Serene 英文提示词写法但中文输出。
- 产出 `首页场景开发提示词.md`：技术栈+Sanctuary 令牌(global) + 六幕逐段描述(Hero 3D地球/Rocket宣言/Knowledge地平线/ Scholars星野/Resources射电/JoinUs静态) + 共有实现要点(共享背景层、<ParticleTitle>复用组件、IntersectionObserver、真实h1 SEO)。令牌锚定真实值：深空#080E21、卡片#0B1533、铜橙#EA6D15、字体Noto Serif SC/Geist。
- 修订 `首页场景开发提示词.md`（用户反馈）：① 首屏 h1 只写"行星科学"（去掉"知识库"）；② 各 section 不放置独立卡片，子页入口改为"纯文字提示(.label-plate，无框无背景块)"；③ 第3幕望远镜旁加工具箱，KNOWLEDGE 拼成后鼠标靠近→箱盖打开→散铜橙微光→淡入"进入学科知识"；④ 新增"可重复动效总原则"小节，用 ScrollTrigger 四回调(onEnter/onLeave/onEnterBack/onLeaveBack)，回流非倒放（流星飞出不出回飞、粒子重建）；⑤ 对所有 2D canvas 图形(火箭/望远镜/工具箱/流星/射电望远镜/卫星/飞船/地球/星野/星座)做详尽造型描述，便于 AI 绘制。
- 再次修订 `首页场景开发提示词.md`：补充**浅色(#F5F3EE 羊皮纸)/深色(#080E21 深渊)双模式**呈现——每幕加"浅色模式呈现"段落；新增第十节双模式对照表 + 实现规则（canvas 颜色必须由 CSS 变量驱动、监听 .dark class 变更重取色、地球 shader 按模式切 uniforms、铜橙#EA6D15 两模式恒定）。

## 落地实现：共享背景层（首个结构性修复）
- 用户说"直接落地试试"。先复查 app/ 接线：React Router + next-themes(.dark class 切换) + Lenis + GSAP ScrollTrigger；HomePage 拼 Hero→Rocket→三 Scene→BottomCta。
- 关键发现：**现有 KnowledgeScene.tsx 已实现提示词大部分细节**（地平线弧、望远镜拖拽、公式流星、KNOWLEDGE 粒子拼字、工具箱 hover 微光入口、浅/深双主题、IntersectionObserver 可重复成形/飞散）。真正缺的是提示词第九/十节强调的**贯穿全页共享背景层**。
- 新增 `app/src/components/SharedSpaceBackground.tsx`：fixed 全屏 canvas，读 CSS 变量(--foreground/--primary/--muted-foreground)取色（hslVarToRgb 借助临时元素 getComputedStyle），深=星场+极淡铜橙星云、浅=墨点星图；主题切换经 MutationObserver 重取色重绘；滚动视差、reduced-motion 静态、隐藏标签页暂停 rAF。
- 修改 `app/src/components/Layout.tsx`：引入 SharedSpaceBackground 置于内容之下(z-0)，内容包进 `relative z-10` 包裹层，root 去 `bg-background`（由 body 提供底色）。
- 验证：`npm run build` 通过(✓ 6.10s, tsc -b + vite 均过)；`npm run dev` 起在 http://localhost:3001/ 返回 200。
- 后续可继续：① 抽 `<ParticleTitle>` 复用组件去三 Scene 重复；② 把三 Scene 的 form/scatter 显式迁到 ScrollTrigger 四回调（当前 IntersectionObserver 阈值已达成"可重复非倒放"，可选）；③ 视差/性能微调。

## 严格对照提示词核查 + 修正（用户批评"动效和提示词不匹配，不要偷懒"）
用户要求逐条对照 `首页场景开发提示词.md` 重新核查，不符的重新绘制/更改动效。本轮落地：
- 新增 `app/src/lib/themeCanvas.ts`：统一取色模块，所有 canvas 颜色由 CSS 变量经 getComputedStyle 读取、随 .dark 切换(MutationObserver)实时重取，禁止写死十六进制（满足 §十规则①）。
- **造型重绘（线框替代实心 SVG）**：RocketManifesto 改为 canvas 程序化线框火箭(铜橙/深蓝墨描边+腹部镂空宣言)；KnowledgeScene 望远镜/工具箱改为 canvas 线框(折射式望远镜+拖拽、矩形箱+拱形提手)；ResourcesScene 卫星(矩形+网格太阳能板+天线)、飞船(十边形+碟形高增益天线+鞭状天线+RTG 支臂)按 §七详尽重绘。
- **动效机制修正（§二/§九）**：Knowledge/Scholars/Resources 三幕的标题成形/飞散，由原先 IntersectionObserver 阈值改回 **GSAP ScrollTrigger 四回调**(onEnter/onEnterBack→聚拢重新实例化；onLeave/onLeaveBack→飞散消解)，IO 仅保留 rAF 离屏暂停。放宽三幕 startForming 守卫，保证快速回流也能重新聚拢(非倒放)。
- **Hero 铜橙种子弧（§三）**：PlanetHero 底部加极淡铜橙弧光(随 scrollProgress 淡入、用 var(--primary))，呼应地球淡出"幽灵圆环"，StaticHero 降级态也加静态弧。
- **工具箱门控（§五）**：KNOWLEDGE 拼成后才出现 hover 微光与"进入学科知识"文字(is-formed class 门控)，光晕/文字用 hsl(var(--primary))。
- **清理硬编码**：删除 RocketManifesto 未用常量 COPPER_FALLBACK；删除死代码 FormingTitle.tsx(含 6 处硬编码铜橙，全工程零引用)；SharedSpaceBackground 初始占位改由 HSL 令牌派生；三幕 COPPER 字面量全部改 getThemeColors().primaryRgb。
- **构建验证**：`npm run build` 通过(✓ 4.70s)。过程中修复两处真实错误——KnowledgeScene 删除未定义的 scopeImgs 引用；ScholarsScene draw 中局部数值变量 `copper` 遮蔽同名取色函数导致 `copper()` 不可调用(tsc 报错)，重命名为 copA。
- 已知未实现项：§九建议抽 `<ParticleTitle>` 复用组件去三 Scene 重复代码（属代码结构优化，不影响视觉/动效与提示词一致性，尚未做）。

## 整轮回退到"重写前"风格（用户推翻上一轮严格对齐提示词的成果）
- 用户原话"现在的模式不如之前的版本……回调回去"，经 AskUserQuestion 确认基线 = `q-0 完全回到重写前风格`：火箭/望远镜/工具箱恢复 SVG 实心剪影(drawImage)、三幕动效恢复 IntersectionObserver 阈值(0.25/0.55)、铜橙硬编码('234,109,21' + 双模式十六进制)、无 Hero 铜橙种子弧、工具箱 Link 去掉 is-formed 门控(hover 常亮)。唯一保留项：上一轮修掉的 KnowledgeScene `scopeImgs` 未定义崩溃 bug（否则望远镜场景一跑就崩）。
- 删除 `app/src/lib/themeCanvas.ts`（严格对齐提示词 §十新建的 CSS 变量取色模块，回退后不再需要）；全工程 Grep 确认除 RocketManifesto 两处过时注释外无残留引用。
- 清理 RocketManifesto.tsx 顶部"线框火箭/themeCanvas"过时注释（改描述 SVG 剪影 + 硬编码取色）。
- **回退后验证**：`npx tsc -b` 通过(EXIT=0)，类型干净（无 themeCanvas/drawTelescopeWire 未使用/残留 copper() 引用）。`vite build` 被沙箱拦截（清理 dist/assets 被识别为批量删除、用户拒绝权限）——属环境权限问题非代码问题，完整打包未跑。

## 调研：优秀制作网站案例的工作流（用户新问）
- 端到端流水线（多家代理商一致）：策略(目标/受众/竞品/KPI + 内容先行) → 设计(线框→UI高保真→设计系统/Token→Figma 交互原型) → **签核 Gate(硬边界)** → 构建(设计工程闭环 + 动效交互) → 交付(Web Vitals/a11y/跨浏览器 → 上线 → 迭代)。
- 范式转变"交接已死"：获奖站采用 Operator 模型（设计=同一人/极小团队在同一循环），"五工具法则"（管线>5 工具每道边界掉保真度），动效是交付物而非润色层。
- Awwwards 2025 对 30 个 SOTY 提名真实统计：创意技术 GSAP21/Lenis18/Three.js18/Howler7/Lottie6/Framer Motion3/Barba2；框架侧 Vue/Nuxt13、Webflow6、纯HTML+JS4、Astro/React/Next/Svelte 各2、WP1。本站(React19+Vite+TS+Tailwind+GSAP+Lenis+Three)与获奖标配同频。
- 项目落地建议：把 `首页场景开发提示词.md` 当**冻结基线/签核 gate**，任何偏离(如严格对齐提示词重写)先作为显式变更请求确认再动手，避免"重写↔回退"来回拉扯；并建立版本快照(git init 或 dated 副本)，使未来回退成为一条命令而非手动重建。

## 文档产出：行星科学网站总体规格说明书
- 用户要求把全站功能/设计需求/技术栈整体写入 Obsidian Vault，且**场景描述与具体细节分离**。
- 产出 `/Users/etteraug/Documents/Obsidian Vault/行星科学网站-总体规格说明书.md`：分**卷一·场景叙事(纯描述,无参数,像提示词)** 与 **卷二·技术规格与设计细节(含颜色值/字体/令牌/技术栈/动效机制/架构/验收基线)**。
- 卷一六幕(Hero/火箭/学科知识/学者星野/外部资源/加入我们)只写体验与故事；卷二承载全部量化参数，并明确标注当前"重写前"冻结基线与理想规格的偏差(造型=SVG剪影、动效=IO阈值、取色=硬编码铜橙)。

## 卷一场景叙事迭代（参考 sanctuaryonthemoon.com）
- 用户反馈上一版卷一"像炫技科技展厅，不符合期待"，要求参考 https://sanctuaryonthemoon.com/ 迭代。
- 该站即 Sanctuary 设计语言灵感源：深蓝(#0B1533)+白+土橘强调、衬线碑文体、圆形=永恒母题、蓝宝石圆盘"知识方舟"隐喻；核心母题"探索与传承：今天探索者把知识镌刻/收纳/递给未来探索者"。
- 重写卷一：整体气质从"太空圣殿/科技"改为"敬畏·安静·恒久的知识圣殿/方舟"；第二幕火箭→"远行的舟/方舟(递给未来的信)"；第三幕→"学科圣殿/分藏的主题之匣"；第五幕→"深空的回声(发往远方的信号)"；第六幕→"跨越时间的邀约"；圆形=永恒作为贯穿母题。卷一仍保持零参数、纯叙事。卷二 2.4 补一句灵感来源。
- 文件：`/Users/etteraug/Documents/Obsidian Vault/行星科学网站-总体规格说明书.md`（已 overwrite 更新）。

## 安装 prompt-engineering-expert 并重写卷一（提示词工程写法）
- 用户问"有提示词相关 skill 吗？需要安装后再书写"。从市场安装 `prompt-engineering-expert`(skillId skill_2053083323789344768, v1.0.0) 到 ~/.workbuddy/skills/；手动安全审计=仅 .md/.json、无脚本/外联/凭据收集，风险 P2 安全。
- 加载该 skill 方法论(BEST_PRACTICES/TECHNIQUES/EXAMPLES)，按提示词工程最佳实践重写卷一：
  - 全局 `<brief>` 结构化：role/objective/core_motif/mood/zero_param_rule/terminology/format/anti_patterns（显式零参数硬规则 + 反模式清单）。
  - 每幕 `<scene>` 结构化：role/objective/mood/narrative/viewer_experience/constraints/acceptance/reference。
  - 保留 Sanctuary「探索与传承/知识圣殿/圆形永恒」气质与零参数要求；卷二逐字保留未动。
- 用户偏好（已验证）：编写场景描述/创意简报类文档前，应先加载 prompt-engineering-expert 方法论再书写。

## 路由数字错误修正（用户指正）+ 文档核查纪律
- 用户质疑卷二 2.1 "共 9 个路由" 不实。经查 `app/src/App.tsx` 真实共 **10 个 `<Route>`**（index 首页 + knowledge/knowledge/:subjectId + scholars/scholars/:scholarId + resources/resources/:resourceId + news/:id + about + * 404）。"9" 系凭转述估计、无代码依据；且 `news` 仅 `news/:id` 详情、**无列表页**。另 2.7 曾把动态参数写成两个重复 `:id`，亦错。
- 已修正 2.1 / 2.2(6) / 2.7 三处，改为真实路由表并标注 news 无列表页缺口。
- 教训（已写入项目 MEMORY.md「文档/规格纪律」）：卷二所有技术陈述须**逐条对照代码核实**，禁止凭旧文档转述或估计数字。

## 卷二逐条核查 + 新增"子页面设计需求"专章（用户要求启动核查 + 汇总子页需求）
- 逐页读取 app/ 源码核实卷二陈述，修正一批纰漏：
  - **2.3 技术栈**：重分类为「实际驱动站点的核心依赖」vs「已装但未接入页面」。实测未接入：gray-matter(用手写 parseFrontmatter)、vanta(VantaHero 存在但全站无引用,首页 Hero 用 Three.js PlanetCanvas)、react-resizable-panels(学者切换未用)、embla(NewsCarousel 手写)、cmdk(无命令面板)、recharts/vaul/react-hook-form+zod/date-fns/react-day-picker/input-otp/sonner 均仅 shadcn 备用原语。版本号据 package.json 改为真实值(react 19.2/vite 7.2/ts ~5.9/three 0.185 等)。
  - **2.2 #7 检索**：实际是 Layout 头部手写 includes 搜索(含键盘导航/热门标签,不含章节正文) + KnowledgeContentPage 内 Fuse.js 搜索；无 cmdk 命令面板。
  - **2.4 令牌**：补 HSL 通道说明(变量以 HSL 存储,如 --primary:24 89% 50%)；`#A68A58` 标注为提示词/参考站规定值(当前未单独取)。
  - **2.7 glob**：`import.meta.glob` 实为分集合精确 globs(scholars/resources/knowledge/*/_index.md/knowledge/*/*.md/news)，非 `**`。
  - **2.11.10 主题机制**：实测非 next-themes 主导,而是自定义 `useTheme` 钩子(`src/hooks/use-theme.ts`)切换 `<html>.dark` + localStorage;next-themes 仅 sonner 配色用。
- 新增 **2.11 子页面设计需求与交互逻辑汇总(源码实测)**：逐页讲清 Layout 全局外壳/搜索、`ScholarsPage` 列表↔网格分段切换(`viewMode` 状态机 + localStorage 记忆 + Featured 恒为卡片 + 国家分区 + 手写 includes 搜索/筛选)、`ScholarDetailPage`、Knowledge 列表/详情(阅读工具栏/目录侧栏/Fuse 搜索/打印)、Resources 列表/详情(标签云/分类)、News(无列表页,仅 NewsCarousel+详情,6s 自动播+clip-path 擦除)、About。学者"列表面板切换"澄清：是 viewMode 分段控件,非 resizable-panels 分栏。
- 文件：`/Users/etteraug/Documents/Obsidian Vault/行星科学网站-总体规格说明书.md`(已 overwrite 更新)。

## 动效规格说明书（用户要求"按动效方式描述首页场景 + 讲清场景衔接"）
- 用户给了一套"动效对齐方法论"（录屏>语言 / 感受词→技术病因对照表 / 指认时刻 / 情绪锚点；助理侧工作流=录制现状→录制参考站→用户批注规格→改代码对比）。本环境不能真正录屏，改用**逐行读源码**产出等价 motion spec 供其批注。
- 产出 `/Users/etteraug/Documents/Obsidian Vault/行星科学网站-动效规格说明书.md`：含①批注指南(把用户感受词映射到具体段落/参数)②全局恒层(SharedSpaceBackground 星场=场景间唯一连续胶水,不随场景切换)③六幕逐幕动效(触发类型 scrub/IO阈值/rAF/hover、时长、缓动、方向)④**场景间衔接总览**(滚动位置表:上一幕退场/本幕进行/下一幕进场信号 + 三条规律:标题接力55%/25%阈值、恒层兜底、无交叉溶解)⑤统一动效语言(E-FADE/IO 0.55/0.25/铜橙点缀)⑥已知动效风险。
- 关键事实(源码核实)：场景间**无交叉淡入**;衔接靠恒定星空 + 各幕标题粒子"拼合(IO≥0.55)/飞散(IO<0.25)"接力;Hero 地球滚动过 0.55 淡成幽灵圆环(此前铜橙种子弧已移除);Knowledge 尾部有显式"地面下沉15%H淡出"scrub 衔接。所有参数据代码非估计。

## 本会话：评估首页第二幕(RocketManifesto)现状
- 用户"纠结"第二幕设计，要求先看现状。读取 RocketManifesto.tsx / HomePage.tsx / KnowledgeScene.tsx / rocket.svg 核实。
- 确认当前第二幕 = 冻结的"重写前"基线（与当日早前回退记录一致）：drawImage 实心 SVG 剪影火箭、ScrollTrigger scrub 单向进度(进→停→出,回流=倒放)、腹部镂空宣言、铜橙硬编码、IO 仅离屏暂停。属用户有意回退,非缺陷。
- 画布 @scene#30("网站设计") 的 ardot 适配器本会话未连接(NO_ADAPTER),评估基于实际实现代码,未能读取该设计节点。
- 设计症结假设(待用户确认):卷一叙事把第二幕定为"远行的舟/方舟(递给未来的信)"——安静恒久的传承隐喻;但当前视觉是字面火箭横穿+标语,且 fly-in→fly-out 使宣言仅短暂停靠窗可读后即离开,与"使命宣言应锚定/驻留"语义冲突。

## 本会话续：把用户口头反馈转译为 kimi code 执行简报
- 用户元指令:所有改动交给 kimi code 做,WorkBuddy 只负责把反馈翻译成 kimi 易读的形式(不亲手改代码)。
- 产出 `首页场景修订简报_kimi_code.md`(工作区根目录):逐条锚定文件/函数,现状→期望→实现要点。覆盖:① 火箭改 canvas 线稿(对齐望远镜,删 drawImage 实心剪影)+ 入场顺滑景深(去 pow4 硬缩放)+ 从 Hero 星球背后飞出联动 + Our Mission 重考量;② Hero 行星用 prompt 生成贴图替换 2k_earth_day/nightmap.jpg(附英文 equirectangular 2:1 prompt);③ 学科 Knowledge 字样离视口即消散(非整幕<0.25)+ 工具箱绕后铰链翻开;④ 学者星野改时间驱动缓亮(非 scrub 随滚动速度)+ 星座用真实星位 + 北斗去除常亮铜橙;⑤ 外部资源电波改同心圆 + 粒子发射前保持旋转曲线(删 startForming 随机方块重置)+ 卫星/飞船放大改线稿 + 入口线稿统一且 hover 才铜橙 + 接收器换新造型 + 消散绑标题离视口;⑥ 全局线稿纪律/铜橙仅 hover/深浅双模式。
- 已读实:ScholarsScene.tsx / ResourcesScene.tsx / PlanetCanvas.tsx 确认上述现状(电波非对称椭圆、粒子 ambient 曲线→forming 方块、地球照片纹理等)。

## 续做：补「线稿统一绘制规范」(用户问"如何让kimi保持统一风格绘制高质量线稿")
- 发现上一版简报只说"以望远镜为标杆"但未抽出标杆配方，且漏了"射电望远镜重绘"(用户明确说射电望远镜线稿不满意，但简报§6只修了电波/粒子/卫星/飞船/接收器，未给碟面几何配方)。
- 根因:全工程无共享线稿模块——scopeLineArt 私有 stroke/seg/ring/V3，ResourcesScene.drawTelescope 又重写一套，卫星/飞船甚至无线稿(圆点/drawImage实心rocket.svg)。
- 产出 `首页线稿统一绘制规范_kimi_code.md`(工作区根目录,与修订简报配套):① 新建唯一共享模块 lineArt.ts(给出 TS 骨架:stroke/seg/ringPath/ellipsePath/makeProjector/V/beginLine,LW={1.5,1.0,0.5}/ALPHA={0.9,0.5,0.25} 全站唯一真值);② 线稿纪律(仅描边+透明填充/三档线宽透明度/round/端点落轮廓/严格对称/相对坐标基准单位/弱透视);③ 铜橙纪律(望远镜finder铜橙=唯一受认可常驻点缀,其余默认--foreground仅hover/激活/电波脉冲才铜橙,取色走CSS变量);④ 各对象几何配方(火箭/工具箱绕后铰链/射电碟/卫星bus+太阳能板+天线/飞船十边舱+高增益碟+RTG/接收器换新/北斗)给出必备特征+比例;⑤ 深浅双模式取色验证;⑥ 与修订简报条目对照表。
- 两份交付:首页场景修订简报_kimi_code.md + 首页线稿统一绘制规范_kimi_code.md,交 kimi code 时两份一并给。

## 用户决策：第二幕火箭改 3D 线框（不再 2D canvas）
- 用户问"第二幕效果还是不满意，是不是用 3D 模型实现更简单"→ 经分析(景深假透视是生硬根因、联动需共享坐标系)给出三选项，用户选 q-0「3D 线框火箭(推荐)」。
- 已改写两份交付文件:① 修订简报§0.1注明火箭是 3D 线框例外;§1.1 改为"同场景 3D,从星球背后真飞出"(优先并入 Hero 的 Three.js 场景做真实遮挡,退路为两画布锁同滚动阈值);§3 整章重写为 3D 线框(程序化几何体+Cylinder/Cone+EdgesGeometry 线框、腹部铜橙环、宣言+Our Mission 改 HTML 浮层、位姿由滚动驱动 easeInOut、删 pow(4))。② 线稿规范§2 加 3D 例外说明、§4.1 火箭改 3D 线框配方、§1 注释去掉火箭的 2D 透视。
- 关键架构点:火箭与 Hero 行星共享 Three.js 场景/相机/滚动时间线,根治"生硬由小变大"与"联动难";线框即线稿,与全站线稿语言一致;rocket.svg 不再用于本幕(飞船也改独立线稿,该 svg 可废弃)。
- 尚未执行:交给 kimi code 后由其实现;若用户要我直接跑 Hero 行星贴图生成 prompt 仍待命。

## 纠正：交付 kimi 改用「增量文档」模式（用户明确批评）
- 用户批评:上一轮我不该直接改写已交付的两份基线 md（修订简报 + 线稿规范）把第二幕改成 3D——这会让 kimi 把整个 md 重新读一遍,应把新要求写成**新文档**。
- 已纠正:① 把两份基线文档里被我改成 3D 的章节(修订简报§0.1/§1.1/§3、线稿规范§1注释/§2注记/§4.1)**还原为 2D 基线**;② 把 3D 方案抽成独立增量文档 `首页第二幕3D方案_增量变更_kimi_code.md`(开头声明覆盖基线哪些章节、以本单为准、自包含完整方案),kimi 只需在已读基线之上增量读这一份。
- 已把该交付纪律写入项目 MEMORY.md「文档/规格纪律」:凡对已交付修订文档提新要求,一律写新增量文档,禁止修改已交付基线 md。
- 当前交付给 kimi 的三份文件:基线2份(2D版,稳定不动) + 增量单1份(第二幕3D方案)。后续任何新要求均追加新增量单,不回头改旧文档。

## version2 续写：工具箱改 3D 线框 + 命名纠正
- 用户要求交付文档统一用 `versionN_描述.md` 命名(不要多套不同名称)。原增量单重命名为 `version2_首页场景3D线框修订.md`(原 `首页第二幕3D方案_增量变更_kimi_code.md`)。
- 用户续加需求(尚未交给 kimi):第三幕**工具箱也改 3D 线框**(与火箭同一 3D 语言),且**撤销原"绕铰链开盖"代码动画**——不要现这种开盖动线,应像望远镜那样作为**地平线上的静态 3D 线框实体**呈现;入口语义保留,hover 反馈改为 3D 线框高亮+铜橙微光+"进入学科知识"浮现(不做开盖)。
- 已在 version2 追加「覆盖四」(覆盖基线§5.2/§4.2),并更新文档标题与开头覆盖范围声明。version2 现含:火箭3D + 工具箱3D 两项。
- 命名/交付纪律已写入项目 MEMORY.md「文档/规格纪律」(versionN 命名 + 增量模式)。

## version2 再次续写（用户又加 5 项需求，全部往 version2 里加，尚未交给 kimi）
- 用户新反馈(对照已读实的 KnowledgeScene/ResourcesScene/ScholarsScene/HomePage 源码)：
  1. **工具箱翻回"动态"**：撤销上一轮"静态地平线"写法 → 改为**动态 3D 线框 + 地平线并列 + 锚点开箱 + 白色内部光**(宝箱从内部发光,严禁铜橙);撤销 KnowledgeScene 现有 `.toolbox-lid` 绕铰链翻转 + `.toolbox-inner-glow` 铜橙微光,改为新增 `toolboxZoneRef` 不可见锚点(IntersectionObserver 阈值~0.5)驱动开箱/合盖。仍是"进入学科知识"入口,可叠 hover 反馈。
  2. **射电望远镜也 3D 线框 + 锚点**：ResourcesScene.drawTelescope 2D 线稿 → Three.js 3D 线框(碟面 Lathe/Cylinder + 馈源 + 塔架 + 配重),新增 `telescopeZoneRef` 锚点互动,保留点击发电波 + 碟面反推;铜橙仅馈源舱。
  3. **卫星/飞船 3D 线稿 + 降频 + 曲线轨迹**：drawSatelliteLineArt/drawShipLineArt → 3D wireframe;卫星取消 `Math.random()<0.005` 每帧成组(改每14–22s 单颗);二者轨迹 `s.x-=v` 直线 → 轻微纵向正弦漂移(呈现曲线)。
  4. **幕间距与过渡缓动空间**：HomePage 直接堆叠各幕(78–100vh),过渡段太短;要求加 30–50vh 过渡带 + 拉长 ScrollTrigger 缓动区间 + 退场/进场解耦。
  5. **SCHOLARS 字形不清**：ScholarsScene.sampleLetters 按真实字形采样,但 buildLetterPaths 用"x列蛇形+最近邻连线"呈网格感;要求提高采样密度(LETTER_COUNT↑ step↓)+ 重画/取消字母内连线,使单词清晰可读,保持星点成字机制。
- 已在 version2 改写标题/覆盖范围声明、重写「覆盖四」(工具箱动态3D+锚点+白光)、新增「覆盖五」(外部资源3D化)、「覆盖六」(幕间距缓动)、「覆盖七」(SCHOLARS字形)、末尾「kimi执行清单(version2全量)」。
- version2 现含:火箭3D + 工具箱(动态3D+锚点+白光) + 射电望远镜3D+锚点 + 卫星/飞船3D+降频+曲线 + 幕间距缓动 + SCHOLARS字形。仍稳定不动的基线:修订简报_kimi_code.md + 线稿统一绘制规范_kimi_code.md(2D版)。
- 关键源码事实(已核实,供 kimi 实现参考):KnowledgeScene 已有 `titleZoneRef` 不可见锚点驱动 KNOWLEDGE 成形/消散(IntersectionObserver 阈值0.55/0.25),工具箱现是 hover 开盖 SVG Link(铜橙微光);ResourcesScene 卫星 `Math.random()<0.005` 每帧、`s.x-=s.v` 直线、飞船每25–40s;ScholarsScene LETTER_COUNT=500、buildLetterPaths 列蛇形最近邻连线、字母星 r≈2.2。

## version2 精简为 AI 友好紧凑版（用户要求省 token 不省意思）
- 用户:交付文档是给 AI(kimi)看的,要精简——去掉背景叙述/元注释/客套,只留「文件:函数 → 现状 → 改法 → 参数」可执行信息,像"我自己最想看的 md"那样。
- 已把 `version2_首页场景3D线框修订.md` 从 ~200 行叙述体重写为 ~50 行紧凑体:开头一行覆盖声明 + 锚点机制一次说明 + 编号对象条目(每条内联 文件/改法/数值参数) + 末尾全局纪律。所有技术实质(路径/函数/阈值/颜色/几何体/状态机)均保留。
- 交付纪律补充:给 kimi 的 md 应默认紧凑、去噪、可执行;冗长解释性文字只对用户有用,对 AI 是噪音。

## version2 加条目7：三幕标题 字间距 & 画面占比 统一
- 用户要求"注意一下 knowledge、scholars、resources 的字间距和画面中的占比"——解读为三幕标题视觉协调一致。
- 已读实三处占比约束现状差异:KNOWLEDGE(KnowledgeScene.tsx:148)宽上限86%/最大1:1/居中;SCHOLARS(ScholarsScene.tsx:588)60%×34%/居中;RESOURCES(ResourcesScene.tsx:135)允许放大1.6倍/中部偏右(非居中)。三者均无独立 letterSpacing(靠字体测量)。
- 已在 version2 加「条目7」:统一占比(宽~72%/高~32%/cx=W/2居中,删 KNOWLEDGE 86%过宽 与 RESOURCES 1.6放大+偏右)、统一字间距(采样canvas设 octx.letterSpacing 同比例)、统一字体(站点衬线);保留各自锚点/成形机制;并同步更新开头覆盖范围声明。
