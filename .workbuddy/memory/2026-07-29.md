# 2026-07-29 工作日志

## 对标站分析校正（用户真机回忆补全）
- 用户补充 sanctuaryonthemoon.com 关键视觉细节，修正了我 07-28 无头抓取的误读：
  - 视觉中心**全程有圆形月球常驻**（非 ~30% 才登场）；~30% 的 0→1 淡入是装饰环层 `HomeMoonCircles`，不是主角。
  - 月球**晨昏线（terminator）随滚动持续推移**（光照/自转被滚动驱动）。
  - 叙事某一刻月球转全黑（夜半球朝观众）瞬间，**翻为白色"源泉"**（发光圆盘），圆形锚点从实体天体转为文明/知识源头。
- 误读根因：抓取环境代理限制导致 3D 贴图 401/连接关闭，月球纹理未渲染，仅采到 DOM 装饰层。用户真机肉眼描述比缺失截图的像素更可靠。
- 已更新 `对标站_视觉编排分析.md`：§三 第4行改判、§四.1 标注误读、新增 §六 校正 + 对首页启示（持久圆形锚点 / 晨昏线随滚动推移 / 全黑→光源收束）。
- 本次未交付 kimi 修订文档；若落地须另开 `versionN_对标借鉴.md` 增量单。

## 环境事实（沿用 07-28）
- Playwright + 无头 Chromium 已装于托管 node22.22.2 workspace；可抓外部站，但代理限制致部分资源 401，WebGL 贴图可能缺。模型本身不能读图，分析靠 DOM 文本探针。

## 代码核实：纠正两条错误记忆（给用户的讲解前读码）
- 用户问"我原本的视觉特效 + 对标站如何渲染 + 如何串联"，先派 Explore 子代理通读六幕源码。
- **纠正**：(1) 不存在 `themeCanvas.ts`/`getThemeColors()`；真·读 CSS 变量取色仅 `SharedSpaceBackground.tsx` 的 `refreshColors()`（全局背景层，+MutationObserver 重绘）；三幕 canvas 场景(Knowledge/Scholars/Resources)实际是每帧 `isDark ? '#fff' : '#0B1533'` **硬编码 hex**，未读变量——与提示词 §十 偏差。(2) 不存在共享 `lineArt.ts`；弱透视投影 `s=F/(F+z)` 是各 `*.ts` 线稿文件内联复制的。
- 已同步修正 MEMORY.md 第9行（双模式取色实际现状）+ 第10行线稿约定未变。
- 已就"我站渲染细节/对标站渲染/六幕串联方案"向用户做口头讲解（未落文档）。若用户要落地串联，须另开 `versionN_视觉串联.md` 增量单。

## version4 交付：火箭绕行星飞出 + 去黑色星空
- 用户两诉求：①火箭从星球背后绕一圈飞出，行星全黑时刚好停在屏幕中央且占比较大范围；②Hero 深色模式多出的黑色星空层（仅深色加载）去除，避免与浅色不一致。
- 读实代码：PlanetCanvas.tsx 的 `StarDome`(142-158行, `{isDark && <StarDome/>}` 299行, 仅深色加载 2k_stars.jpg 黑底天空盒) = 用户说的"另一个黑色星空"；火箭投影 `s=F/(F+zOff)`，`zOff` 需到 `Z_FAR≈F*15.66` 量级才有远小近大(rocketLineArt.ts)。Hero 行星 fade 在首屏前 0.8vh(scrollProgress 0.55→1)，Rocket section 紧随。
- 产出 `version4_首页火箭绕行星飞出与去黑色星空.md`：§1 删 StarDome 组件+调用，保留 StarField+SharedSpaceBackground；§2 重写 `pose()` 入场为绕 C=(0.5W,0.46H) 螺旋一周(φ=π/2+2πu, Rs=0.34min(W,H)(1-u), zOff=Z_FAR(1-u)^1.6, alpha 随 zOff 淡入)，ORBIT_END=0.45 落中央；L 由 min(W*0.62,560) 提至 min(W*0.86,820) 居中放大；时序靠 section 顺序自然衔接(行星全黑→火箭居中)。可选增强：火箭 canvas 移入 Hero 由 scrollProgress 驱动(未做)。
- 未改代码/基线；kimi 据此增量实现。
