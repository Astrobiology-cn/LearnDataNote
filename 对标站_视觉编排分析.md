# 对标站视觉编排分析：sanctuaryonthemoon.com

> 用途：为「行星科学网站首页」修订提供叙事/动效参考。
> 来源：用 Playwright + 无头 Chromium 实际加载该站，真实滚轮驱动其虚拟滚动容器，按 0%→100% 分 24 桶采样元素状态重建，非读取源码。

## 一、抓取方式与环境
- 工具：Playwright（装于托管 node22.22.2 workspace）+ Chromium（SwiftShader 软件 WebGL）。
- 驱动：该站**不滚动 window**（documentElement 高度仅 900），改用虚拟滚动容器 `.Site-scrollmain`，靠 `transform: translateY` 平移内容。用 `page.mouse.wheel` 派发真实滚轮事件驱动，轮询 translateY 到目标进度后采样。
- 采样：每个进度桶截图一帧 + 用 DOM 探针提取各 section 的 `getBoundingClientRect().top` 与 `getComputedStyle().opacity`，以及视口内可见的 `h1/h2/h3/p` 文字。
- 产出：`playwright_capture/frames/` 下 25 张图 + `timeline.json` + `index.html` 帧预览。

## 二、已知限制（务必知悉）
- **模型本身不支持读图**，故分析基于 DOM 文本数据而非像素视觉；截图仅供用户肉眼校对。
- 抓取环境代理对并发连接有限制，部分**图片/字体/3D 贴图缺失**（大量 401 / ERR_CONNECTION_CLOSED）。动画时序、布局、文字、opacity 不受此影响，但月球真实纹理、各 section 配图、WebGL 球体细节在截图中可能不全——真机浏览器无此问题。

## 三、叙事结构（进度 → 视口内容 → 元素状态）

| 进度 | 视口核心内容 | 关键元素 |
|---|---|---|
| 0–8% | Hero 主标题 + 赞助人信息（IN COLLABORATION WITH / Under the High Patronage of / Mr Emmanuel MACRON） | `Home_hero` 在视口，op=1 |
| 8–21% | 过渡段，右下角音频播放器 `00:00` 常驻 | hero 文字淡出，进入空白过渡 |
| 21–29% | 标语 "SANCTUARY ON THE MOON IS ABOUT EXPLORATION AND TRANSMISSION." + "THE EXPLORERS OF TODAY PASSING DOWN A CONCENTRATE OF KNOWLEDGE TO THE EXPLORERS OF THE FUTURE." | `HomeContext` 进视口（top 由 +220 转负） |
| ≈30–33% | **装饰圆环层 `HomeMoonCircles` 透明度 0→1 淡入**（注意：此层非主角；真实月球球体因纹理 401 未被抓取，见 §六） | op 在 P29=0、P33=1，迟到的装饰环登场 |
| 33–46% | "A LEGACY / FOR THE GENERATIONS TO COME / A VAULT / FOR KNOWLEDGE AND CULTURE. FOR ETERNITY. / 24 DISCS / MADE OF SAPPHIRE" | `HomeDiscs` 进视口（P42 top=597→P50 top=-27） |
| 54–67% | 圆盘说明 "The complete female/male genome selected in a double-blind population study." | HomeDiscs 详情 |
| 67–83% | "A TESTIMONIAL OF / GENOME, SCIENCE AND ART / A PHILOSOPHICAL AND ANTHROPOLOGICAL BREAKTHROUGH" | `HomePictures` 进视口（P67 top=368→P75 top=-257） |
| 92–100% | footer：Sanctuary On The Moon / All rights reserved / Made by | `partners`+`HomePress` 进视口 |

补充：贯穿前 ~52% 滚动有一个长背景层 `Home_hero`（高 3764），类似本站 Hero 行星作为背景层贯穿首屏后段。

## 四、关键动画手法（可借鉴）
1. **主角延迟登场（部分误读，见 §六校正）**：原判读为"月球约 30% 进度才淡入"，实为**装饰圆环层** `HomeMoonCircles` 在 ~30% 淡入；真实月球球体始终居中常驻（文本淡出后仍可见），并非延迟登场。
2. **文字成段随滚动浮现**：每段大标语在对应 section 进入视口时浮现，离开时自然移出——与本站 Knowledge/Scholars 的 ScrollTrigger 进场同源，可保持。
3. **视差分层**：不同层 translateY 速度不同（月球层反向 +718 vs 滚动容器 -718）。本站资源幕的电波/卫星已有位移感，可加轻微视差增强纵深。
4. **持久常驻元素**：音频播放器/导航在全程固定，提供锚点。

## 五、对首页修订的建议（待定，未交付 kimi）
- 本站与对标站叙事结构高度同源（多幕分散 + 滚动进场 + 背景天体层），修订方向一致，无需大改范式。
- 视差层次可适度加强，尤其资源幕。
- 本分析仅作参考输入；若需落地为 kimi 修订指令，应另开增量文档（遵循 versionN 命名纪律），不回头改本文件。

## 六、用户校正：视觉中心恒有圆形（月球）+ 晨昏线变化 + 全黑变白源泉
> 2026-07-29 用户基于真机浏览器观察的口头校正，覆盖 §三 第 4 行与 §四.1 的误读。无头抓取的 401 缺失使月球纹理未渲染，故以下细节以用户肉眼回忆为准（比截图可靠）。

1. **视觉中心始终有一个圆形**：整段滚动过程中，画面正中一直有一颗月球（圆形天体），并非 §三 所记的"约 30% 才登场"。装饰圆环层 `HomeMoonCircles` 的 0→1 淡入是叠加在月球之上的额外几何环，不是主角本身。
2. **晨昏线随滚动持续变化**：月球的**晨昏线（terminator，昼夜分界线）随滚动进度一直在移动/变化**——即光照角度或月球自转在滚动中被驱动，昼半球与夜半球的边界不断推移，营造"实时被照亮"的活体感。这是核心动效之一，也是抓取时因贴图缺失完全漏掉的部分。
3. **全黑瞬间 → 白色源泉**：在叙事推进到某一刻（月球转到完全背光的"全黑"状态、即夜半球完全朝向观众）的瞬间，黑色月面**转变为一个白色的"源泉"**（发光圆盘/泉眼），成为视觉收束点。圆形锚点在此刻从"被照亮的实体"转为"发光的源头"，完成语义切换（实体天体 → 知识/文明的源泉）。

### 对首页修订的启示（待定）
- **持久圆形锚点**：对标站"全程居中圆形"与本站 Hero 首屏即有地球的方向一致，建议保持"视觉中心恒有圆形天体"的锚定感，不必学"延迟登场"。
- **可引入「晨昏线随滚动推移」**：本站 Hero 的 3D 地球目前是静态贴图 + 滚动到 0.55 淡成幽灵圆环。可借鉴：让地球**晨昏线/光照随滚动进度缓慢推移**（驱动地球自转轴或光源角度），增强"活体行星"感——这比纯淡入淡出更有叙事张力。
- **「全黑→光源」收束**：本站目前没有"末态收束成一个白色源泉"的设计；若未来要在某幕（如 Join Us / 终幕）做语义升华，可参考此手法——把深色圆面在临界点翻为发光白色圆，作为"源泉/起点"的视觉母题。
- 以上均为参考输入；若落地为 kimi 修订指令，须另开 `versionN_对标借鉴.md` 增量单，不回头改本文件。
